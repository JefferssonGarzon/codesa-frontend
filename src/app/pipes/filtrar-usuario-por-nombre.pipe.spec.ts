import { FiltrarUsuarioPorNombrePipe } from './filtrar-usuario-por-nombre.pipe';

describe('FiltrarUsuarioPorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarUsuarioPorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
